{
  "name": "wifi-slide",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "slidev --open --remote",
    "build": "slidev build",
    "build-base": "slidev build --out ../../dist/wifi-slide --base /wifi-slide/",
    "export": "slidev export --with-clicks --per-slide --wait-until none"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
